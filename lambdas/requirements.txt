# Runtime dependencies for Lambda functions

# Anthropic SDK - kept for rollback capability
# Comment out to reduce bundle size when using Mistral only
anthropic>=0.40.0

# AWS SDK - boto3 is provided by Lambda runtime, pinned for local dev
# Also used for Bedrock Mistral invocation
boto3>=1.34.0

# Lambda utilities
aws-lambda-powertools>=2.32.0
aws-xray-sdk>=2.12.0

# Data validation
pydantic>=2.5.0
